use starknet::{get_block_timestamp};
use pistols::models::models::{Challenge};
use pistols::utils::timestamp::{timestamp};
use pistols::types::challenge::{ChallengeState};


fn shoot(ref challenge: Challenge) {
}
