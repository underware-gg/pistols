#!/bin/bash
set -e
source scripts/setup.sh

echo "@WORLD: $WORLD_ADDRESS"

if [[
  "$WORLD_ADDRESS" != "0x"*
]]; then
  echo "! Missing data 👎"
  exit 1
fi

torii --version
torii \
  --world $WORLD_ADDRESS $* \
  --allowed-origins "*" --index-pending \

